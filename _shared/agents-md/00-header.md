# AGENTS.md — Editor-Agnostic Agent Behavior

> **Canonical source.** This file is read by Claude Code (via `CLAUDE.md`), Cursor (via `.cursor/rules/index.mdc`), and any other AGENTS-aware agent harness. Editor-specific supplements (slash commands, harness-specific tooling, skill-routing tables) live alongside this file in `CLAUDE.md` and never duplicate the rules here.

